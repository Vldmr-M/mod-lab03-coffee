//"Copyright 2025 Owner"
#include "Automata.h"

int main() {
    Automata automata;
    automata.on();
    automata.getState();
    automata.getMenu();
    automata.coin(500);
    automata.choice(0);
    automata.check();
    automata.cook();
    automata.finish();
    automata.off();

    return 0;
}
